apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config
  namespace: FreeTheIdServer
spec:
  accessModes:
  - ReadOnlyMany
  volumeMode: Filesystem
  resources:
   requests:
    storage: 10Mi